# Optional: Amap Web Service API key for address lookup and driving routes.
# Without it, built-in locations, metro planning and rough driving estimates still work.
# Copy this file to .env.local, then restart `bun run dev`.
# Server-only: never use a VITE_ prefix and never commit a real key.
AMAP_WEB_SERVICE_KEY=
