# Optional: Amap Web Service API key. Leave empty to use built-in metro/business areas.
# Copy this file to .env.local, then restart `bun run dev`.
# Server-only: never use a VITE_ prefix and never commit a real key.
AMAP_WEB_SERVICE_KEY=
